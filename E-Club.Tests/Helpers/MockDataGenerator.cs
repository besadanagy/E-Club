using Bogus;
using E_Club.Models;

namespace E_Club.Tests.Helpers;

public static class MockDataGenerator
{
    // Example for ApplicationUser if needed
    // public static Faker<ApplicationUser> UserFaker => new Faker<ApplicationUser>()
    //     .RuleFor(u => u.Id, f => f.Random.Guid().ToString())
    //     .RuleFor(u => u.UserName, f => f.Internet.UserName())
    //     .RuleFor(u => u.Email, f => f.Internet.Email())
    //     .RuleFor(u => u.FirstName, f => f.Name.FirstName())
    //     .RuleFor(u => u.LastName, f => f.Name.LastName());

    // You can add more fakers for your other models like Event, Announcement, Service here.
}
